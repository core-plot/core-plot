{
	Actions = {
		// Define action descriptions here, for example
		// "myAction:" = id;
		action: = id;
	};
	Outlets = {
		// Define outlet descriptions here, for example
		// myOutlet = NSView;
	};
    ClassName = KTViewControl; 
    SuperClass = KTView; 
}
