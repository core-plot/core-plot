Creating a Core Plot Installer Package
--------------------------------------

1) Build the SDK using the scripts/createrelease.py script.
2) Move the SDK to the packaging directory, and rename 'CorePlot'
3) Change into the packaging directory in Terminal, and issue this command

sudo ./prepare.sh

4) Open the Core Plot Installer package project and create the package
5) Remove the temporary Library directory